%h1 Event Instances
= render partial: "common/sieve_search_box"
%ul.no-bullet.sieve
  - @event_instances.each do |instance|
    %li.row
      .small-10.medium-8.columns
        =instance.date
        =link_to instance.name, instance.event
        - if instance.cancelled?
          Cancelled
      .small-2.medium-4.columns.actions
        = link_to instance.url, class: "button tiny" do
          %span.button-text View Site
          %i.fi-arrow-right

